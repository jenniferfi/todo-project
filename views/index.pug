extends layout

block content
  div(id="box1" class="container")
    h1= title

    form#form(onsubmit="addItem(event)")
      input#item(type="text" placeholder="Add new item to the list" name="item" required)
      button#button(type="submit") Add

  div(id="box2" class="container")
    table#list
      tbody#tbl

  script(src='../javascripts/scripts.js')